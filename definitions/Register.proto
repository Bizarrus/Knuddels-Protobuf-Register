syntax = "proto3";

import "Gender.proto";
import "RequestType.proto";
import "Data.proto";

message Commit {
	Bundle packet = 1;
	
	message Bundle {
		string action = 1;
		Data data = 2;
	}
}

message Check {
	Bundle packet = 1;
	
	message Bundle {
		string action = 1;
		Data data = 2;
	}
}